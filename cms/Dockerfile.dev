FROM node:22-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends python3 make gcc g++ && rm -rf /var/lib/apt/lists/*
COPY package.json package-lock.json ./
RUN npm install
EXPOSE 1337
ENV CHOKIDAR_USEPOLLING=true
CMD ["npm", "run", "develop"]
